#include <stdio.h>

int main() {
    
    // pointer
    // pointer는 주소에 대한 개념
    // pointer variable는 일반적인 variable과 다르게 address를 저장
    // int *p = &variable;
    // 위와 같은 형태로 사용, variable name 앞에 *이 있으면 pointer variable

    // int a = 10;
    // int *p = &a;            // a의 address를 저장
    // printf("%d \n", *p);    // *p에는 a의 address가 저장되어 있어 a의 값을 가지고 올 수 있음


    return 0;

}